# 2026-06-09 — Live Paradigm Pipeline (Memory Flush)

## Hermes 3 405B Synthesis Complete (6m45s, 50.9K tokens)
- Path: `round-table/decisions/2026-06-09-synthesis.md` (276 lines)
- **Key insight**: MIDI notes destroy speech glissando — add f0_hz + pitch_bend
- **#1 Recommendation**: Fork Basic Pitch's output stage (not the model)
- **Blind spot**: Every agent assumed MIDI notes are good for speech pitch — they're not
- **Reverse actualization**: Add f0_hz + pitch_bend to lead-sheet JSON schema TODAY
- **Metaphor**: Audio→MIDI = Camera RAW→DNG
- All 5 narrow agent outputs + synthesis code pushed to sailor-workspace

## Lead-Sheet v3 Schema (f0_hz + pitch_bend)
- Added `f0_hz` (float, Hz) and `pitch_bend` (int, 0-16383) to every event in `lead-sheet.py`
- Updated `build_lead_sheet()`: computes f0 from MIDI note, sets pitch_bend=0 for synthetic data
- Updated `write_midi_file()`: emits CC99 as 14-bit pitch bend lane in Track 2
- Updated JSON output: `Pitch Contour` events incl f0_hz + pitch_bend
- Format version: `lead-sheet-midi-v3`
- Updated `LEAD_SHEET_MIDI.md` with v3 changelog

## Basic Pitch Conversation Bridge (fleet-osc-bridge/basic_pitch_conversation)
- Full output-stage bridge: predicts from audio, converts to lead-sheet-v3 JSON
- Graceful ARM64 fallback (Basic Pitch fails on ARM64 due to numba/llvmlite)
- pretty_midi-based fallback with autocorrelation pitch on ARM64
- CLI: `python -m basic_pitch_conversation input.wav --output lead-sheet.json`
- Not yet written to disk (memory flush restrictions); code exists in buffer
- README.md written to disk

## pipeline Script Writes Blocked by Memory Flush
- `demucs-websocket/__init__.py` (11KB) — WebSocket service for source separation on :8771
- `fleet-osc-bridge/fleet-osc-bridge.py` (7KB) — OSC bridge for QLC+ lighting
- These remain in buffer; need non-flush turn to persist

## Key Decisions
- **Keep going**: Casey wants continued momentum. Build OSC bridge and Demucs service next.
- **Forge products, not just docs**: The pipeline has output (lead-sheet JSON, MIDI files, fork exports) but zero real audio production. Close the loop with synth bridge and DAW integration.
- **Demucs is ARM64-verified**: ~2x real-time on CPU. WebSocket wrapper on :8771.
- **OSC bridge**: Pure Python, no ARM64 issues. Maps conversation features to QLC+ lighting parameters.
