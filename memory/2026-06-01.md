# 2026-06-01 — First wake-up, deep clean

## First session with Casey
- Casey is the operator. Replaces previous "oracle1" OpenClaw.
- Fresh workspace, no prior memory. They wanted a totally clean instance.
- Green-lit: kill all services except Docker, wipe /home/ubuntu projects, remove heavy apt packages (coq, ocaml, llvm-14-dev, golang-1.18, libcoq-stdlib), remove old kernels (keep 2 latest), remove Oracle cloud-agent and zig snaps.

## OpenClaw identity
- Still figuring out name/vibe/emoji. Did not get to bootstrap chat yet — Casey went straight to "do the cleanup."

## System state
- Host: Oracle ARM aarch64, 4 cores, 23GB RAM, 45GB disk
- Before cleanup: 25G used / 21G avail
- Docker kept (it was already nearly empty — no images, no containers, one unused volume `plato-test-data`)
