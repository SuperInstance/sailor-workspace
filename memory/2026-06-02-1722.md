# Session: 2026-06-02 17:22:45 UTC

- **Session Key**: agent:main:telegram:direct:8709904335
- **Session ID**: 2ee2a5da-f4b4-4f59-8850-ccf3c44d185c
- **Source**: telegram

## Conversation Summary

assistant: This is a goldmine. Let me read the rest and the operator runbook:
assistant: Let me read the runbook section (the most actionable bit):
assistant: **This is the runbook I'll ship.** Let me also look at the critical install fix and the http_api auth gap specifically:
assistant: I already wrote a doctor.py that does most of this. Let me check what's in the systemd section:
assistant: OK I've got enough. Let me now act on the highest-priority findings. Order of attack:

1. **CRITICAL: HTTP API auth** — anyone on the network can `/run` and `/teach`
2. **CRITICAL: install.sh uses pip install -r not -e, breaking the systemd unit** — but this might not actually be true on the local install
3. **CRITICAL: systemd unit hardcodes /home/ubuntu/lever-runner and User=ubuntu** — won't be portable
4. **CRITICAL: install.sh runs init_db.py --reset, wiping data** — verify
5. **HIGH: systemd hardening knobs** — MemoryMax, ProtectSystem, etc.

Let me verify the local install first, then fix the http_api:
assistant: Confirmed. The console script IS installed locally (because the agent did `pip install -e .` during v0.3.1), but `install.sh` does `pip install -r requirements.txt` which doesn't install the script. So a fresh `bash install.sh` would have a broken unit. **Critical.**

Let me fix all 4 criticals in order. Start with the http_api (the one that's exploitable):
assistant: Now the fix. Strategy:
1. Bind to **127.0.0.1 by default** (loopback only)
2. Optional `HTTP_API_TOKEN` env var → Bearer token auth
3. Make `/run` and `/teach` require auth when token is set
4. `/healthz` and `/status` stay unauthenticated (monitoring needs them)
5. Read-only operations: `/run` is read-only (it runs a pre-approved command), `/teach` is the dangerous one
assistant: Now add token check to the POST handlers:
assistant: Now the main() function — refuse to start if non-loopback + no token:
assistant: Let me also fix the install.sh, systemd unit, and init_db reset:
assistant: Now also fix the systemd unit — make it portable, add hardening:
assistant: Now let me also fix `init_db.py --reset` to be safer:
assistant: The `--reset` already exists. The issue is install.sh *always* passes it. I've already fixed install.sh to not pass it. Let me also add a safety guard: `--reset` should require a confirmation flag in non-interactive contexts. Actually, simpler: make `--reset` require `--yes` to actually drop:
assistant: Now run smoke to make sure I didn't break anything (the http_api change and the init_db change need testing):
assistant: ⚠️ Something went wrong while processing your request. Please try again, or use /new to start a fresh session.
